const express = require("express");
const router = express.Router();
const interestControllers = require("../models/interestModels.js");

// CRUD-endpoints

// Får interesse
router.get("/", (req, res) => {
});

// Sletter interesse
router.delete("/", (req, res) =>{
});

// Opdaterer interesse
router.put("/", (req, res) => {
});

// Opretter en interesse
router.post("/", (req, res) => {
});

        module.exports = router;


   module.exports = router;